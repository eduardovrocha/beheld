node_modules/
dist/
*.js
*.d.ts
!packages/cli/assets/beheld-engine

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.eggs/
packages/engine/dist/
packages/engine/build/
packages/engine/*.spec

# Engine binary (generated by build, not committed)
packages/cli/assets/beheld-engine

# OS
.DS_Store

# Separate project — Rails portal + Docker (lives at web/, tracked independently)
web/

# Local working docs / mockups — not part of the published repo
/documents/

# Local tool artifacts — Claude Code config, graphify output, session notes
.claude/
graphify-*/
*-historico.md
next-session.md
commits-raw.txt

# Playwright test results (local CI artifacts)
test-results/
