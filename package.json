{
  "name": "devprofile",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "build": "sh scripts/build.sh",
    "build:cli": "bun build packages/cli/src/index.ts --compile --outfile dist/devprofile"
  }
}
