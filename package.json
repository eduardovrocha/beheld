{
  "name": "beheld",
  "version": "0.3.2",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "build": "sh scripts/build.sh",
    "build:cli": "bun build packages/cli/src/index.ts --compile --outfile dist/beheld",
    "sync:platform-keys": "bun run scripts/sync-platform-keys.ts"
  }
}
