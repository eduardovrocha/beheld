{
  "name": "@devprofile/cli",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "build": "bun build src/index.ts --compile --outfile ../../dist/devprofile"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
