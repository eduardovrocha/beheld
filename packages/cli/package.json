{
  "name": "@beheld/cli",
  "version": "0.3.2",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "build": "bun build src/index.ts --compile --outfile ../../dist/beheld"
  },
  "dependencies": {
    "@sigstore/sign": "^4.1.1",
    "commander": "^12.1.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
